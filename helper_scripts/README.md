# LYDO Monitoring — Helper Scripts

This folder contains standalone helper scripts (previously `backend/`) for local CLI and testing.

## Quick start
Run the CLI from the project root:

```bash
python helper_scripts/lydo_backend.py
```

Database file: `helper_scripts/monitor.db`

The rest of the README is identical to the original but paths updated to `helper_scripts/`.
